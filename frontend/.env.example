# Backend API (Render). Leave empty to run the UI in demo mode with seed data.
NEXT_PUBLIC_API_URL=

# Supabase (Auth). Leave empty to skip auth in demo mode.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
